@using proyecto_hospital_version_1.Models // Aunque no se use Paciente, es buena práctica tenerlo si el parámetro existe

<div class="card alert-widget-sticky shadow-sm">
    <div class="card-body">
        <h5 class="card-title mb-3"><i class="bi bi-exclamation-triangle-fill me-2"></i> Alertas Clínicas Clave</h5>
        
        @* Este componente NO ES FUNCIONAL y SOLO MUESTRA EJEMPLOS ESTÁTICOS por ahora.
           El parámetro 'Paciente' se acepta, pero no se utiliza para determinar las alertas.
           Esto se hará en una fase posterior de desarrollo. *@

        <p class="alert-item ges mb-1">⚠️ Paciente GES</p>
        <p class="alert-item alergia mb-1">❗ Alergia a Penicilina</p>
        <p class="alert-item">🩸 Diabetes Tipo 2</p>
        <p class="alert-item">❤️ Historial Cardíaco</p>
        <p class="alert-item prais mb-0">⭐ Paciente PRAIS</p> 
    </div>
</div>

<style>
    .alert-widget-sticky {
        position: sticky;
        top: 20px;
    }
    .alert-item {
        padding: 5px 10px;
        margin-bottom: 5px;
        border-radius: 5px;
        font-size: 0.9em;
        background-color: #ffeeba; /* Color de fondo general para alertas */
        color: #856404; /* Color de texto general para alertas */
        border: 1px solid #ffeeba;
    }
    /* Estilos específicos para tipos de alerta si los quieres, basados en tu código anterior */
    .alert-item.ges { background-color: #f8d7da; color: #721c24; border-color: #f5c6cb; } /* Rojo suave */
    .alert-item.alergia { background-color: #fff3cd; color: #856404; border-color: #ffeeba; } /* Amarillo suave */
    .alert-item.prais { background-color: #d1ecf1; color: #0c5460; border-color: #bee5eb; } /* Azul suave */
    /* Añadir más estilos si es necesario */
</style>

@code {
    // Aceptamos el parámetro Paciente, pero no lo usamos para la lógica de las alertas
    // ya que este componente es estático por ahora.
    [Parameter]
    public PacienteHospital? Paciente { get; set; }
}